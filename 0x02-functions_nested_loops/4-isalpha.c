#include <stdlib.h>
#include <unistd.h>

int _isalpha(int c);

int main(void)
{
	int d= 2;
	_isalpha(int c);
	_putchar('\n');
	 return (0);
}
int _isalpha(int c);
{  
	  if (isalpha(c))
	  {
		  return 1;
	  }
	  else
	  {
		  return 0;
	  }

}
